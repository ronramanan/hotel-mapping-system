{
  "name": "hotel-mapping-lambda",
  "version": "2.0.0",
  "description": "Hotel Mapping System Lambda Handler with Complete Country Support",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . -x '*.git*' 'node_modules/.cache/*' && aws lambda update-function-code --function-name hotel-mapping-api --zip-file fileb://function.zip --region ap-southeast-1",
    "logs": "aws logs tail /aws/lambda/hotel-mapping-api --follow --region ap-southeast-1"
  },
  "keywords": [
    "hotel",
    "mapping",
    "lambda",
    "aws",
    "postgresql"
  ],
  "author": "Kamoota Group",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.515.0",
    "@aws-sdk/s3-request-presigner": "^3.515.0",
    "pg": "^8.11.3",
    "csv-parser": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
